<jsp:include page="contentHeader.jsp">
<jsp:param name="docTitle" value="CodeArchives - SQL" />
</jsp:include>

<img src="archive/resources/images/topics/sql.png"/>

<!-- start content here -->
<table class="infobox vevent" style="width: 684px; height: 495px;" cellspacing="5">
	<caption class="summary" style="">
		&nbsp;</caption>
	<tbody>
		<tr>
			<th scope="row" style="text-align: left;">
				<span>Paradigm</span></th>

			<td style="">
				<span class="mw-redirect">Multi-paradigm</span></td>
		</tr>
		<tr>
			<th scope="row" style="text-align: left;">
				Appeared in</th>
			<td style="">
				1974</td>

		</tr>
		<tr>
			<th scope="row" style="text-align: left;">
				Designed by</th>
			<td class="organiser" style="">
				<span>Donald D. Chamberlin</span><br>
				<span>Raymond F. Boyce</span></td>

		</tr>
		<tr>
			<th scope="row" style="text-align: left;">
				<span>Developer</span></th>
			<td style="">
				<span>IBM</span></td>
		</tr>
		<tr>

			<th scope="row" style="text-align: left;">
				<span>Stable release</span></th>
			<td style="">
				SQL:2008 (2008)</td>
		</tr>
		<tr>
			<th scope="row" style="text-align: left;">
				<span>Typing discipline</span></th>

			<td style="">
				<span class="mw-redirect">Static</span>, <span>strong</span></td>
		</tr>
		<tr>
			<th scope="row" style="text-align: left;">
				<span>Major implementations</span></th>
			<td style="">

				<span>Many</span></td>
		</tr>
		<tr>
			<th scope="row" style="text-align: left;">
				<span>Dialects</span></th>
			<td style="">
				SQL-86, SQL-89, <span>SQL-92</span>, <span>SQL:1999</span>, <span>SQL:2003</span>, <span>SQL:2008</span></td>

		</tr>
		<tr>
			<th scope="row" style="text-align: left;">
				Influenced by</th>
			<td style="">
				<span>Datalog</span></td>
		</tr>
		<tr>

			<th scope="row" style="text-align: left;">
				Influenced</th>
			<td style="">
				<span>Agena</span>, <span class="mw-redirect">CQL</span>, <span>LINQ</span>, <span>Windows PowerShell</span><sup style="display:none;" class="Template-Fact" style="white-space: nowrap;">[<i><span>citation needed</span></i>]</sup></td>

		</tr>
		<tr>
			<th scope="row" style="text-align: left;">
				<span>OS</span></th>
			<td style="">
				<span>Cross-platform</span></td>
		</tr>
		<tr>

			<th scope="row" style="text-align: left;">
				Usual <span>file extensions</span></th>
			<td style="">
				.sql</td>
		</tr>
	</tbody>
</table>
<table class="infobox" style="width: 683px; height: 449px;" cellspacing="5">

	<caption style="">
		&nbsp;</caption>
	<tbody>

		<tr>
			<th scope="row" style="text-align: left;">
				<span>Filename extension</span></th>
			<td style="">
				<code>.sql</code></td>
		</tr>
		<tr>
			<th scope="row" style="text-align: left;">

				<span>Internet media type</span></th>
			<td style="">
				<code>application/x-sql</code></td>
		</tr>
		<tr>
			<th scope="row" style="text-align: left;">
				Developed by</th>

			<td style="">
				<span>IBM</span></td>
		</tr>
		<tr>
			<th scope="row" style="text-align: left;">
				Initial release</th>
			<td style="">
				1986<span style="display: none;">&nbsp;(<span class="bday dtstart published updated">1986</span>)</span></td>

		</tr>
		<tr>
			<th scope="row" style="text-align: left;">
				<span>Latest release</span></th>
			<td style="">
				SQL:2008 / 2008<span class="noprint">; 2 years ago</span><span style="display: none;">&nbsp;(<span class="bday dtstart published updated">2008</span>)</span></td>

		</tr>
		<tr>
			<th scope="row" style="text-align: left;">
				Type of format</th>
			<td style="">
				Database</td>
		</tr>
		<tr>

			<th scope="row" style="text-align: left;">
				<span>Standard(s)</span></th>
			<td style="">
				ISO/IEC 9075</td>
		</tr>
		<tr>
			<th scope="row" style="text-align: left;">
				<span>Open format</span>?</th>

			<td style="">
				Yes</td>
		</tr>
	</tbody>
</table>
<p>
	<b>SQL</b> (officially pronounced <span class="IPA"><span>/??skju?'?l/</span></span> like "S-Q-L" but often pronounced <span class="IPA"><span>/'si?kw?l/</span></span> like "sequel"),<sup style="display:none;" class="reference" id="cite_ref-0"><span><span>[</span>1<span>]</span></span></sup> often referred to as <b>Structured Query Language</b>, is a <span>database</span> computer language designed for managing <span>data</span> in <span class="mw-redirect">relational database management systems</span> (RDBMS), and originally based upon <span>relational algebra</span> and <span>calculus</span>.<sup style="display:none;" class="reference" id="cite_ref-Darwen_1-0"><span><span>[</span>2<span>]</span></span></sup> Its scope includes data insert, query, update and delete, <span>schema</span> creation and modification, and data access control. SQL was one of the first commercial languages for <span>Edgar F. Codd</span>'s <span>relational model</span>, as described in his influential 1970 paper, "A Relational Model of Data for Large Shared Data Banks".<sup style="display:none;" class="reference" id="cite_ref-codd-relational-model_2-0"><span><span>[</span>3<span>]</span></span></sup> Despite not adhering to <span>the relational model as described by Codd</span>, it became the most widely used database language.<sup style="display:none;" class="reference" id="cite_ref-SQL-Fundamentals_3-0"><span><span>[</span>4<span>]</span></span></sup><sup style="display:none;" class="reference" id="cite_ref-IBM-sql_4-0"><span><span>[</span>5<span>]</span></span></sup></p>

<h2>
	<span class="mw-headline" id="History">History</span></h2>
<p>
	SQL was developed at <span>IBM</span> by <span>Donald D. Chamberlin</span> and <span>Raymond F. Boyce</span> in the early 1970s. This version, initially called <b>SEQUEL</b> (<b>S</b>tructured <b>E</b>nglish <b>Que</b>ry <b>L</b>anguage), was designed to manipulate and retrieve data stored in IBM's original quasi-relational database management system, <span>System R</span>, which a group at <span>IBM San Jose Research Laboratory</span> had developed during the 1970s.<sup style="display:none;" class="reference" id="cite_ref-chamberlin-boyce-sequel_5-0"><span><span>[</span>6<span>]</span></span></sup> The acronym SEQUEL was later changed to SQL because "SEQUEL" was a <span>trademark</span> of the <span>UK-based</span> <span>Hawker Siddeley</span> aircraft company.<sup style="display:none;" class="reference" id="cite_ref-oppel-databases_6-0"><span><span>[</span>7<span>]</span></span></sup></p>

<p>
	The first <i>Relational Database Management System</i> (RDBMS) was <span class="mw-redirect">RDMS</span>, developed at <span class="mw-redirect">MIT</span> in the early 1970s, soon followed by <span>Ingres</span>, developed in 1974 at <span>U.C. Berkeley</span>. Ingres implemented a query language known as <span>QUEL</span>, which was later supplanted in the marketplace by SQL.<sup style="display:none;" class="reference" id="cite_ref-oppel-databases_6-1"><span><span>[</span>7<span>]</span></span></sup></p>

<p>
	In the late 1970s, Relational Software, Inc. (now <span>Oracle Corporation</span>) saw the potential of the concepts described by Codd, Chamberlin, and Boyce and developed their own SQL-based <span class="mw-redirect">RDBMS</span> with aspirations of selling it to the <span>U.S. Navy</span>, <span>Central Intelligence Agency</span>, and other <span>U.S. government</span> agencies. In June 1979, Relational Software, Inc. introduced the first commercially available implementation of SQL, <span class="mw-redirect">Oracle</span> V2 (Version2) for <span>VAX</span> computers. <i>Oracle V2</i> beat IBM's August release of the <span class="mw-redirect">System/38</span> RDBMS to market by a few weeks.<sup style="display:none;" class="Template-Fact" style="white-space: nowrap;">[<i><span>citation needed</span></i>]</sup></p>

<p>
	After testing SQL at customer test sites to determine the usefulness and practicality of the system, IBM began developing commercial products based on their System R prototype including System/38, <span class="mw-redirect">SQL/DS</span>, and <span>DB2</span>, which were commercially available in 1979, 1981, and 1983, respectively.<sup style="display:none;" class="reference" id="cite_ref-IBM-history_7-0"><span><span>[</span>8<span>]</span></span></sup></p>
<h2>
	<span class="mw-headline" id="Language_elements">Language elements</span></h2>
<p>
	The SQL language is sub-divided into several language elements, including:</p>

<ul>
	<li>
		<b>Clauses</b>, which are constituent components of statements and queries. (In some cases, these are optional.)<sup style="display:none;" class="reference" id="cite_ref-8"><span><span>[</span>9<span>]</span></span></sup></li>
	<li>
		<b>Expressions</b>, which can produce either <span>scalar</span> values or <span>tables</span> consisting of <span>columns</span> and <span>rows</span> of data.</li>

	<li>
		<b>Predicates</b>, which specify conditions that can be evaluated to SQL <span class="mw-redirect">three-valued logic (3VL)</span> or <span>Boolean</span> (true/false/unknown) <span>truth values</span> and which are used to limit the effects of statements and queries, or to change program flow.</li>
	<li>

		<b>Queries</b>, which retrieve the data based on specific criteria. This is the most important element of <b>SQL</b>.</li>
	<li>
		<b>Statements</b>, which may have a persistent effect on schemas and data, or which may control transactions, program flow, connections, sessions, or diagnostics.
		<ul>
			<li>
				SQL statements also include the <span>semicolon</span> (";") statement terminator. Though not required on every platform, it is defined as a standard part of the SQL grammar.</li>

		</ul>
	</li>
	<li>
		<b><span class="mw-redirect">Insignificant whitespace</span></b> is generally ignored in SQL statements and queries, making it easier to format SQL code for readability.</li>
</ul>
<h3>
	<span class="mw-headline" id="Queries">Queries</span></h3>
<p>

	The most common operation in SQL is the query, which is performed with the declarative <code><span>SELECT</span></code> statement. <code>SELECT</code> retrieves data from one or more <span>tables</span>, or expressions. Standard <code>SELECT</code> statements have no persistent effects on the database. Some non-standard implementations of <code>SELECT</code> can have persistent effects, such as the <code>SELECT INTO</code> syntax that exists in some databases.<sup style="display:none;" class="reference" id="cite_ref-ms-sql-select-into_9-0"><span><span>[</span>10<span>]</span></span></sup></p>

<p>
	Queries allow the user to describe desired data, leaving the <span>database management system (DBMS)</span> responsible for <span>planning</span>, <span>optimizing</span>, and performing the physical operations necessary to produce that result as it chooses.</p>
<p>
	A query includes a list of columns to be included in the final result immediately following the <code>SELECT</code> keyword. An asterisk ("<code>*</code>") can also be used to specify that the query should return all columns of the queried tables. <code>SELECT</code> is the most complex statement in SQL, with optional keywords and clauses that include:</p>

<ul>
	<li>
		The <code><span>FROM</span></code> clause which indicates the table(s) from which data is to be retrieved. The <code>FROM</code> clause can include optional <code><span>JOIN</span></code> subclauses to specify the rules for joining tables.</li>
	<li>

		The <code><span>WHERE</span></code> clause includes a comparison predicate, which restricts the rows returned by the query. The <code>WHERE</code> clause eliminates all rows from the result set for which the comparison predicate does not evaluate to True.</li>
	<li>
		The <code><span class="mw-redirect">GROUP BY</span></code> clause is used to project rows having common values into a smaller set of rows. <code>GROUP BY</code> is often used in conjunction with SQL aggregation functions or to eliminate duplicate rows from a result set. The <code>WHERE</code> clause is applied before the <code>GROUP BY</code> clause.</li>

	<li>
		The <code><span>HAVING</span></code> clause includes a predicate used to filter rows resulting from the <code>GROUP BY</code> clause. Because it acts on the results of the <code>GROUP BY</code> clause, aggregation functions can be used in the <code>HAVING</code> clause predicate.</li>

	<li>
		The <code><span class="mw-redirect">ORDER BY</span></code> clause identifies which columns are used to sort the resulting data, and in which direction they should be sorted (options are ascending or descending). Without an <code>ORDER BY</code> clause, the order of rows returned by an SQL query is undefined.</li>
</ul>
<p>
	The following is an example of a <code>SELECT</code> query that returns a list of expensive books. The query retrieves all rows from the <i>Book</i> table in which the <i>price</i> column contains a value greater than 100.00. The result is sorted in ascending order by <i>title</i>. The asterisk (*) in the <i>select list</i> indicates that all columns of the <i>Book</i> table should be included in the result set.</p>

<div class="mw-geshi" dir="ltr" style="text-align: left;">
	<div class="sql source-sql" style="font-family: monospace;">
		<pre class="de1"><span class="kw1">SELECT</span> <span class="sy0">*</span>
    <span class="kw1">FROM</span> Book
    <span class="kw1">WHERE</span> price <span class="sy0">&gt;</span> 100<span class="sy0">.</span>00
    <span class="kw1">ORDER</span> <span class="kw1">BY</span> title;

</pre>
	</div>
</div>
<p>
	The example below demonstrates a query of multiple tables, grouping, and aggregation, by returning a list of books and the number of authors associated with each book.</p>
<div class="mw-geshi" dir="ltr" style="text-align: left;">
	<div class="sql source-sql" style="font-family: monospace;">
		<pre class="de1"><span class="kw1">SELECT</span> Book<span class="sy0">.</span>title<span class="sy0">,</span>

        count<span class="br0">(</span><span class="sy0">*</span><span class="br0">)</span> <span class="kw1">AS</span> Authors
    <span class="kw1">FROM</span>  Book <span class="kw1">JOIN</span> Book_author
       <span class="kw1">ON</span> Book<span class="sy0">.</span>isbn <span class="sy0">=</span> Book_author<span class="sy0">.</span>isbn
    <span class="kw1">GROUP</span> <span class="kw1">BY</span> Book<span class="sy0">.</span>title;

</pre>
	</div>
</div>
<p>
	Example output might resemble the following:</p>
<pre>Title                   Authors
----------------------  -------
SQL Examples and Guide     4
The Joy of SQL             1
An Introduction to SQL     2
Pitfalls of SQL            1
</pre>
<p>
	Under the precondition that <i>isbn</i> is the only common column name of the two tables and that a column named <i>title</i> only exists in the <i>Books</i> table, the above query could be rewritten in the following form:</p>

<div class="mw-geshi" dir="ltr" style="text-align: left;">
	<div class="sql source-sql" style="font-family: monospace;">
		<pre class="de1"><span class="kw1">SELECT</span> title<span class="sy0">,</span>
        count<span class="br0">(</span><span class="sy0">*</span><span class="br0">)</span> <span class="kw1">AS</span> Authors
    <span class="kw1">FROM</span>  Book <span class="kw1">NATURAL</span> <span class="kw1">JOIN</span> Book_author
    <span class="kw1">GROUP</span> <span class="kw1">BY</span> title;

</pre>
	</div>
</div>
<p>
	However, many vendors either do not support this approach, or require certain column naming conventions in order for natural joins to work effectively.</p>
<p>
	SQL includes operators and functions for calculating values on stored values. SQL allows the use of expressions in the <i>select list</i> to project data, as in the following example which returns a list of books that cost more than 100.00 with an additional <i>sales_tax</i> column containing a sales tax figure calculated at 6% of the <i>price</i>.</p>

<div class="mw-geshi" dir="ltr" style="text-align: left;">
	<div class="sql source-sql" style="font-family: monospace;">
		<pre class="de1"><span class="kw1">SELECT</span> isbn<span class="sy0">,</span>
        title<span class="sy0">,</span>
        price<span class="sy0">,</span>

        price <span class="sy0">*</span> 0<span class="sy0">.</span>06 <span class="kw1">AS</span> sales_tax
    <span class="kw1">FROM</span> Book
    <span class="kw1">WHERE</span> price <span class="sy0">&gt;</span> 100<span class="sy0">.</span>00
    <span class="kw1">ORDER</span> <span class="kw1">BY</span> title;

</pre>
	</div>
</div>
<h4>
	<span class="mw-headline" id="Null_and_three-valued_logic_.283VL.29">Null and three-valued logic (3VL)</span></h4>
<p>
	The idea of <span>Null</span> was introduced into SQL to handle missing information in the relational model. The introduction of Null (or Unknown) along with True and False is the foundation of <span>three-valued logic</span>. Null does not have a value (and is not a member of any data domain) but is rather a placeholder or "mark" for missing information. Therefore comparisons with Null can never result in either True or False but always in the third logical result.<sup style="display:none;" class="reference" id="cite_ref-SQL2003-Part2-Sec87_10-0"><span><span>[</span>11<span>]</span></span></sup></p>

<p>
	SQL uses Null to handle missing information. It supports three-valued logic (3VL) and the rules governing SQL three-valued logic are shown below (p and q represent logical states).<sup style="display:none;" class="reference" id="cite_ref-fourrules_11-0"><span><span>[</span>12<span>]</span></span></sup> The word NULL is also a reserved keyword in SQL, used to identify the Null special marker.</p>
<p>
	Additionally, since SQL operators return Unknown when comparing anything with Null, SQL provides two Null-specific comparison predicates: <code>IS NULL</code> and <code>IS NOT NULL</code> test whether data is or is not Null.<sup style="display:none;" class="reference" id="cite_ref-SQL2003-Part2-Sec87_10-1"><span><span>[</span>11<span>]</span></span></sup></p>

<p>
	Note that SQL returns only results for which the WHERE clause returns a value of True; i.e. it excludes results with values of False, but also those whose value is Unknown.</p>

<p>
	<span>Universal quantification</span> is not explicitly supported by SQL, and must be worked out as a negated <span>existential quantification</span>.<sup style="display:none;" class="reference" id="cite_ref-12"><span><span>[</span>13<span>]</span></span></sup><sup style="display:none;" class="reference" id="cite_ref-13"><span><span>[</span>14<span>]</span></span></sup><sup style="display:none;" class="reference" id="cite_ref-kawash_14-0"><span><span>[</span>15<span>]</span></span></sup></p>

<p>
	There is also the "&lt;row value expression&gt; IS DISTINCT FROM &lt;row value expression&gt;" infixed comparison operator which returns TRUE unless both operands are equal or both are NULL. Likewise, IS NOT DISTINCT FROM is defined as "NOT (&lt;row value expression&gt; IS DISTINCT FROM &lt;row value expression&gt;)".</p>

<h3>
	<span class="mw-headline" id="Data_manipulation">Data manipulation</span></h3>
<p>
	The <span>Data Manipulation Language</span> (DML) is the subset of SQL used to add, update and delete data:</p>
<ul>
	<li>
		<code><span>INSERT</span></code> adds rows (formally <span>tuples</span>) to an existing table, e.g.,:</li>

</ul>
<div class="mw-geshi" dir="ltr" style="text-align: left;">
	<div class="sql source-sql" style="font-family: monospace;">
		<pre class="de1"><span class="kw1">INSERT</span> <span class="kw1">INTO</span> My_table
        <span class="br0">(</span>field1<span class="sy0">,</span> field2<span class="sy0">,</span> field3<span class="br0">)</span>

    <span class="kw1">VALUES</span>
        <span class="br0">(</span><span class="st0">'test'</span><span class="sy0">,</span> <span class="st0">'N'</span><span class="sy0">,</span> <span class="kw1">NULL</span><span class="br0">)</span>;
</pre>
	</div>

</div>
<ul>
	<li>
		<code><span>UPDATE</span></code> modifies a set of existing table rows, e.g.,:</li>
</ul>
<div class="mw-geshi" dir="ltr" style="text-align: left;">
	<div class="sql source-sql" style="font-family: monospace;">
		<pre class="de1"><span class="kw1">UPDATE</span> My_table
    <span class="kw1">SET</span> field1 <span class="sy0">=</span> <span class="st0">'updated value'</span>

    <span class="kw1">WHERE</span> field2 <span class="sy0">=</span> <span class="st0">'N'</span>;
</pre>
	</div>
</div>
<ul>
	<li>
		<code><span>DELETE</span></code> removes existing rows from a table, e.g.,:</li>

</ul>
<div class="mw-geshi" dir="ltr" style="text-align: left;">
	<div class="sql source-sql" style="font-family: monospace;">
		<pre class="de1"><span class="kw1">DELETE</span> <span class="kw1">FROM</span> My_table
    <span class="kw1">WHERE</span> field2 <span class="sy0">=</span> <span class="st0">'N'</span>;

</pre>
	</div>
</div>
<ul>
	<li>
		<code><span>MERGE</span></code> is used to combine the data of multiple tables. It combines the <code>INSERT</code> and <code>UPDATE</code> elements. It is defined in the SQL:2003 standard; prior to that, some databases provided similar functionality via different syntax, sometimes called "<span>upsert</span>".</li>

</ul>
<h3>
	<span class="mw-headline" id="Transaction_controls">Transaction controls</span></h3>
<p>
	Transactions, if available, wrap DML operations:</p>
<ul>
	<li>
		<code>START TRANSACTION</code> (or <code>BEGIN WORK</code>, or <code>BEGIN TRANSACTION</code>, depending on SQL dialect) mark the start of a <span>database transaction</span>, which either completes entirely or not at all.</li>

	<li>
		<code>SAVE TRANSACTION</code> (or <code>SAVEPOINT</code> ) save the state of the database at the current point in transaction</li>
</ul>
<div class="mw-geshi" dir="ltr" style="text-align: left;">
	<div class="sql source-sql" style="font-family: monospace;">
		<pre class="de1"><span class="kw1">CREATE</span> <span class="kw1">TABLE</span> tbl_1<span class="br0">(</span>id int<span class="br0">)</span>;
  <span class="kw1">INSERT</span> <span class="kw1">INTO</span> tbl_1<span class="br0">(</span>id<span class="br0">)</span> value<span class="br0">(</span>1<span class="br0">)</span>;
  <span class="kw1">INSERT</span> <span class="kw1">INTO</span> tbl_1<span class="br0">(</span>id<span class="br0">)</span> value<span class="br0">(</span>2<span class="br0">)</span>;
COMMIT;
  <span class="kw1">UPDATE</span> tbl_1 <span class="kw1">SET</span> id<span class="sy0">=</span>200 <span class="kw1">WHERE</span> id<span class="sy0">=</span><span class="nu0">1</span>;
SAVEPOINT id_1upd;
  <span class="kw1">UPDATE</span> tbl_1 <span class="kw1">SET</span> id<span class="sy0">=</span>1000 <span class="kw1">WHERE</span> id<span class="sy0">=</span><span class="nu0">2</span>;
ROLLBACK <span class="kw1">TO</span> id_1upd;
  <span class="kw1">SELECT</span> id <span class="kw1">FROM</span> tbl_1;

</pre>
	</div>
</div>
<ul>
	<li>
		<code><span class="mw-redirect">COMMIT</span></code> causes all data changes in a transaction to be made permanent.</li>
	<li>
		<code><span class="mw-redirect">ROLLBACK</span></code> causes all data changes since the last <code>COMMIT</code> or <code>ROLLBACK</code> to be discarded, leaving the state of the data as it was prior to those changes.</li>

</ul>
<p>
	Once the <code>COMMIT</code> statement completes, the transaction's changes cannot be rolled back.</p>
<p>
	<code>COMMIT</code> and <code>ROLLBACK</code> terminate the current transaction and release data locks. In the absence of a <code>START TRANSACTION</code> or similar statement, the semantics of SQL are implementation-dependent. Example: <i>A classic bank transfer of funds transaction.</i></p>

<div class="mw-geshi" dir="ltr" style="text-align: left;">
	<div class="sql source-sql" style="font-family: monospace;">
		<pre class="de1">START TRANSACTION;
  <span class="kw1">UPDATE</span> Account <span class="kw1">SET</span> amount<span class="sy0">=</span>amount<span class="sy0">-</span>200 <span class="kw1">WHERE</span> account_number<span class="sy0">=</span><span class="nu0">1234</span>;
  <span class="kw1">UPDATE</span> Account <span class="kw1">SET</span> amount<span class="sy0">=</span>amount<span class="sy0">+</span>200 <span class="kw1">WHERE</span> account_number<span class="sy0">=</span><span class="nu0">2345</span>;

<span class="kw1">IF</span> ERRORS<span class="sy0">=</span>0 COMMIT;
<span class="kw1">IF</span> ERRORS<span class="sy0">&lt;&gt;</span>0 ROLLBACK;
</pre>
	</div>
</div>
<h3>
	<span class="mw-headline" id="Data_definition">Data definition</span></h3>

<p>
	The <span>Data Definition Language</span> (DDL) manages table and index structure. The most basic items of DDL are the <code>CREATE</code>, <code>ALTER</code>, <code>RENAME</code>, <code>DROP</code> and <code>TRUNCATE</code> statements:</p>

<ul>
	<li>
		<code><span class="mw-redirect">CREATE</span></code> creates an object (a table, for example) in the database, e.g.,:</li>
</ul>
<div class="mw-geshi" dir="ltr" style="text-align: left;">
	<div class="sql source-sql" style="font-family: monospace;">
		<pre class="de1"><span class="kw1">CREATE</span> <span class="kw1">TABLE</span> My_table<span class="br0">(</span>

    my_field1   INT<span class="sy0">,</span>
    my_field2   VARCHAR<span class="br0">(</span>50<span class="br0">)</span><span class="sy0">,</span>
    my_field3   DATE         <span class="kw1">NOT</span> <span class="kw1">NULL</span><span class="sy0">,</span>

    <span class="kw1">PRIMARY</span> <span class="kw1">KEY</span> <span class="br0">(</span>my_field1<span class="sy0">,</span> my_field2<span class="br0">)</span>
<span class="br0">)</span>;
</pre>
	</div>

</div>
<ul>
	<li>
		<code><span class="mw-redirect">ALTER</span></code> modifies the structure of an existing object in various ways, for example, adding a column to an existing table or a constraint, e.g.,:</li>
</ul>
<div class="mw-geshi" dir="ltr" style="text-align: left;">
	<div class="sql source-sql" style="font-family: monospace;">
		<pre class="de1"><span class="kw1">ALTER</span> <span class="kw1">TABLE</span> My_table <span class="kw1">ADD</span> my_field4 NUMBER<span class="br0">(</span>3<span class="br0">)</span> <span class="kw1">NOT</span> <span class="kw1">NULL</span>;

</pre>
	</div>
</div>
<ul>
	<li>
		<code><span>TRUNCATE</span></code> deletes all data from a table in a very fast way, deleting the data inside the table and not the table itself. It usually implies a subsequent COMMIT operation, i.e., it cannot be rolled back.</li>
</ul>
<div class="mw-geshi" dir="ltr" style="text-align: left;">
	<div class="sql source-sql" style="font-family: monospace;">
		<pre class="de1">
<span class="kw1">TRUNCATE</span> <span class="kw1">TABLE</span> My_table;
</pre>
	</div>
</div>
<ul>
	<li>
		<code><span class="mw-redirect">DROP</span></code> deletes an object in the database, usually irretrievably, i.e., it cannot be rolled back, e.g.,:</li>

</ul>
<div class="mw-geshi" dir="ltr" style="text-align: left;">
	<div class="sql source-sql" style="font-family: monospace;">
		<pre class="de1"><span class="kw1">DROP</span> <span class="kw1">TABLE</span> My_table;
</pre>
	</div>
</div>
<h3>
	<span class="mw-headline" id="Data_types">Data types</span></h3>

<p>
	Each column in an SQL table declares the type(s) that column may contain. ANSI SQL includes the following datatypes.<sup style="display:none;" class="reference" id="cite_ref-SQL1992_15-0"><span><span>[</span>16<span>]</span></span></sup></p>
<h4>
	<span class="mw-headline" id="Character_strings">Character strings</span></h4>
<ul>
	<li>
		<code>CHARACTER(<var>n</var>)</code> or <code>CHAR(<var>n</var>)</code> - fixed-width <var>n</var>-character string, padded with spaces as needed</li>

	<li>
		<code>CHARACTER VARYING(<var>n</var>)</code> or <code>VARCHAR(<var>n</var>)</code> - variable-width string with a maximum size of <var>n</var> characters</li>

	<li>
		<code>NATIONAL CHARACTER(<var>n</var>)</code> or <code>NCHAR(<var>n</var>)</code> - fixed width string supporting an international character set</li>
	<li>

		<code>NATIONAL CHARACTER VARYING(<var>n</var>)</code> or <code>NVARCHAR(<var>n</var>)</code> - variable-width <code>NCHAR</code> string</li>
</ul>

<h4>
	<span class="mw-headline" id="Bit_strings">Bit strings</span></h4>
<ul>
	<li>
		<code>BIT(<var>n</var>)</code> - an array of <var>n</var> bits</li>

	<li>
		<code>BIT VARYING(<var>n</var>)</code> - an array of up to <var>n</var> bits</li>
</ul>
<h4>
	<span class="mw-headline" id="Numbers">Numbers</span></h4>

<ul>
	<li>
		<code>INTEGER</code> and <code>SMALLINT</code></li>
	<li>
		<code>FLOAT</code>, <code>REAL</code> and <code>DOUBLE PRECISION</code></li>

	<li>
		<code>NUMERIC(<var>precision</var>, <var>scale</var>)</code> or <code>DECIMAL(<var>precision</var>, <var>scale</var>)</code></li>
</ul>

<p>
	SQL provides a function to round numerics or dates, called <code>TRUNC</code> (in Informix, DB2, PostgreSQL, Oracle and MySQL) or <code>ROUND</code> (in Informix, Sybase, Oracle, PostgreSQL and Microsoft SQL Server)<sup style="display:none;" class="reference" id="cite_ref-Jones_127_16-0"><span><span>[</span>17<span>]</span></span></sup></p>
<h4>
	<span class="mw-headline" id="Date_and_time">Date and time</span></h4>

<ul>
	<li>
		<code>DATE</code> - for date values (e.g., <code>2010-05-30</code>)</li>
	<li>
		<code>TIME</code> - for time values (e.g., <code>14:55:37</code>). The granularity of the time value is usually a <i>tick</i> (100 nanoseconds).</li>

	<li>
		<code>TIME WITH TIME ZONE</code> or <code>TIMESTAMP</code>&nbsp;- the same as <code>TIME</code>, but including details about the time zone in question.</li>
	<li>
		<code>TIMESTAMP</code> - This is a <code>DATE</code> and a <code>TIME</code> put together in one variable (e.g., <code>2010-05-30 14:55:37</code>).</li>

	<li>
		<code>TIMESTAMP WITH TIME ZONE</code> or <code>TIMESTAMPTZ</code> - the same as <code>TIMESTAMP</code>, but including details about the time zone in question.</li>
</ul>
<p>
	SQL provides several functions for generating a date / time variable out of a date / time string (<code>TO_DATE</code>, <code>TO_TIME</code>, <code>TO_TIMESTAMP</code>), as well as for extracting the respective members (seconds, for instance) of such variables. The current system date / time of the database server can be called by using functions like <code>NOW</code>.</p>

<h3>
	<span class="mw-headline" id="Data_control">Data control</span></h3>
<p>
	The <span>Data Control Language</span> (DCL) authorizes users and groups of users to access and manipulate data. Its two main statements are:</p>
<ul>
	<li>
		<code>GRANT</code> authorizes one or more users to perform an operation or a set of operations on an object.</li>

	<li>
		<code>REVOKE</code> eliminates a grant, which may be the default grant.</li>
</ul>
<p>
	Example:</p>
<div class="mw-geshi" dir="ltr" style="text-align: left;">
	<div class="sql source-sql" style="font-family: monospace;">
		<pre class="de1">
<span class="kw1">GRANT</span> <span class="kw1">SELECT</span><span class="sy0">,</span> <span class="kw1">UPDATE</span>
    <span class="kw1">ON</span> My_table
    <span class="kw1">TO</span> some_user<span class="sy0">,</span> another_user;
 

<span class="kw1">REVOKE</span> <span class="kw1">SELECT</span><span class="sy0">,</span> <span class="kw1">UPDATE</span>
    <span class="kw1">ON</span> My_table
    <span class="kw1">FROM</span> some_user<span class="sy0">,</span> another_user;

</pre>
	</div>
</div>
<h3>
	<span class="mw-headline" id="Procedural_extensions">Procedural extensions</span></h3>
<p>
	SQL is designed for a specific purpose: to query <span>data</span> contained in a <span>relational database</span>. SQL is a <span>set</span>-based, <span>declarative</span> query language, not an <span>imperative language</span> such as <span>C</span> or <span class="mw-redirect">BASIC</span>. However, there are extensions to Standard SQL which add <span class="mw-redirect">procedural programming language</span> functionality, such as control-of-flow constructs. These include:</p>

<table class="acatable" style="width: 806px; height: 272px;" border="1">
	<tbody>
		<tr>
			<th>
				Source</th>
			<th>
				Common<br>
				Name</th>

			<th>
				Full Name</th>
		</tr>
		<tr>
			<td align="center">
				ANSI/ISO Standard</td>
			<td align="center">
				<span>SQL/PSM</span></td>

			<td>
				SQL/Persistent Stored Modules</td>
		</tr>
		<tr>
			<td align="center">
				<span class="mw-redirect">Interbase</span>/<br>
				<span>Firebird</span></td>

			<td align="center">
				<span class="mw-redirect">PSQL</span></td>
			<td>
				Procedural SQL</td>
		</tr>
		<tr>
			<td align="center">
				IBM</td>

			<td align="center">
				<span>SQL PL</span></td>
			<td>
				SQL Procedural Language (implements SQL/PSM)</td>
		</tr>
		<tr>
			<td align="center">
				<span>Microsoft</span>/<br>

				<span>Sybase</span></td>
			<td align="center">
				<span>T-SQL</span></td>
			<td>
				Transact-SQL</td>
		</tr>
		<tr>

			<td align="center">
				<span>Mimer SQL</span></td>
			<td align="center">
				<span>SQL/PSM</span></td>
			<td>
				SQL/Persistent Stored Module (implements SQL/PSM)</td>
		</tr>

		<tr>
			<td align="center">
				<span>MySQL</span></td>
			<td align="center">
				<span>SQL/PSM</span></td>
			<td>
				SQL/Persistent Stored Module (implements SQL/PSM)</td>

		</tr>
		<tr>
			<td align="center">
				<span>Oracle</span></td>
			<td align="center">
				<span>PL/SQL</span></td>
			<td>
				Procedural Language/SQL (based on <span>Ada</span>)</td>

		</tr>
		<tr>
			<td align="center">
				<span>PostgreSQL</span></td>
			<td align="center">
				<span>PL/pgSQL</span></td>
			<td>
				Procedural Language/PostgreSQL Structured Query Language (based on Oracle PL/SQL)</td>

		</tr>
		<tr>
			<td align="center">
				<span>PostgreSQL</span></td>
			<td align="center">
				<span class="new">PL/PSM</span></td>
			<td>
				Procedural Language/Persistent Stored Modules (implements SQL/PSM)</td>

		</tr>
	</tbody>
</table>
<p>
	In addition to the standard SQL/PSM extensions and proprietary SQL extensions, procedural and <span class="mw-redirect">object-oriented</span> programmability is available on many SQL platforms via DBMS integration with other languages. The SQL standard defines <span>SQL/JRT</span> extensions (SQL Routines and Types for the Java Programming Language) to support <span>Java</span> code in SQL databases. <span class="mw-redirect">SQL Server 2005</span> uses the <span class="mw-redirect">SQLCLR</span> (SQL Server Common Language Runtime) to host managed <span class="mw-redirect">.NET</span> assemblies in the database, while prior versions of SQL Server were restricted to using unmanaged extended stored procedures which were primarily written in C. Other database platforms, like MySQL and Postgres, allow functions to be written in a wide variety of languages including <span>Perl</span>, <span>Python</span>, <span>Tcl</span>, and C.</p>

<h2>
	<span class="mw-headline" id="Criticisms_of_SQL">Criticisms of SQL</span></h2>
<p>
	SQL is a <span>declarative computer language</span> intended for use with relational databases. Many of the original SQL features were inspired by, but violated, the semantics of the <span>relational model</span> and its <span>tuple calculus</span> realization. Recent extensions to SQL achieved relational completeness, but have worsened the violations, as documented in <i><span>The Third Manifesto</span></i>. Therefore, it cannot be considered relational in any significant sense, but is still widely called relational due to differentiation to other, pre-relational database languages which never intended to implement the relational model; due to its historical origin; and due to the use of the relational term as propaganda by product vendors.</p>

<p>
	Other criticisms of SQL include:</p>
<ul>
	<li>
		Implementations are inconsistent with the standard and, usually, incompatible between vendors. In particular date and time syntax, string concatenation, <code>NULL</code>s, and comparison <span>case sensitivity</span> vary from vendor to vendor. A particular exception is <span>PostgreSQL</span>, which strives for compliance, and <span>SQLite</span>, which strives to follow PostgreSQL.</li>

	<li>
		The language makes it too easy to do a <span>Cartesian</span> join (joining all possible combinations), which results in "run-away" result sets when <code>WHERE</code> clauses are mistyped. Cartesian joins are so rarely used in practice that requiring an explicit <code>CARTESIAN</code> keyword may be warranted. (<i>SQL 1992</i> introduced the <code>CROSS JOIN</code> keyword that allows the user to make clear that a Cartesian join is intended, but the shorthand "comma-join" with no predicate is still acceptable syntax, which still invites the same mistake.)</li>

	<li>
		It is also possible to misconstruct a <code>WHERE</code> on an update or delete, thereby affecting more rows in a table than desired. (A work-around is to use transactions or habitually type in the WHERE clause first, then fill in the rest later.)</li>
	<li>
		The grammar of SQL is perhaps unnecessarily complex, borrowing a <span>COBOL</span>-like keyword approach, when a function-influenced syntax could result in more re-use of fewer grammar and syntax rules.</li>
	<li>

		The non-compliance of SQL to the relational model, and specifically to the 0th rule of <span>Codd's twelve rules</span>, is another source of complexity and incompatibility.</li>
</ul>
<h3>
	<span class="mw-headline" id="Cross-vendor_portability">Cross-vendor portability</span></h3>
<p>
	Popular implementations of SQL commonly omit support for basic features of Standard SQL, such as the <code>DATE</code> or <code>TIME</code> data types. The most obvious such examples, and incidentally the most popular commercial, proprietary SQL DBMSs, are Oracle (whose <code>DATE</code> behaves as <code>DATETIME</code>,<sup style="display:none;" class="reference" id="cite_ref-Ora_DATE_17-0"><span><span>[</span>18<span>]</span></span></sup><sup style="display:none;" class="reference" id="cite_ref-Ora_DATETIME_18-0"><span><span>[</span>19<span>]</span></span></sup> and lacks a <code>TIME</code> type<sup style="display:none;" class="reference" id="cite_ref-Ora_TIME_19-0"><span><span>[</span>20<span>]</span></span></sup>) and the MS SQL Server. As a result, SQL code can rarely be ported between database systems without modifications.</p>

<p>
	There are several reasons for this lack of portability between database systems:</p>
<ul>
	<li>
		The complexity and size of the SQL standard means that most implementors do not support the entire standard.</li>
	<li>
		The standard does not specify database behavior in several important areas (e.g., <span>indexes</span>, file storage,...), leaving implementations to decide how to behave.</li>

	<li>
		The SQL standard precisely specifies the syntax that a conforming database system must implement. However, the standard's specification of the semantics of language constructs is less well-defined, leading to ambiguity.</li>
	<li>
		Many database vendors have large existing customer bases; where the SQL standard conflicts with the prior behavior of the vendor's database, the vendor may be unwilling to break <span>backward compatibility</span>.</li>
	<li>
		Software vendors often desire to create incompatibilities with other products, as it provides a strong incentive for their existing users to remain loyal (see <span>vendor lock-in</span>).</li>

</ul>
<h2>
	<span class="mw-headline" id="Standardization">Standardization</span></h2>
<p>
	SQL was adopted as a standard by the <span>American National Standards Institute</span> (ANSI) in 1986 as SQL-86<sup style="display:none;" class="reference" id="cite_ref-20"><span><span>[</span>21<span>]</span></span></sup> and <span>International Organization for Standardization</span> (ISO) in 1987. The original SQL standard declared that the official pronunciation for SQL is "es queue el".<sup style="display:none;" class="reference" id="cite_ref-SQL-Fundamentals_3-1"><span><span>[</span>4<span>]</span></span></sup> Many English-speaking database professionals still use the nonstandard<sup style="display:none;" class="reference" id="cite_ref-21"><span><span>[</span>22<span>]</span></span></sup> pronunciation <span class="IPA">/'si?kw?l/</span> (like the word "sequel").</p>

<p>
	Until 1996, the <span>National Institute of Standards and Technology</span> (NIST) data management standards program certified SQL DBMS compliance with the SQL standard. Vendors now self-certify the compliance of their products.<sup style="display:none;" class="reference" id="cite_ref-22"><span><span>[</span>23<span>]</span></span></sup></p>
<p>
	The SQL standard has gone through a number of revisions, as shown below:</p>
<table class="acatable">
	<tbody>

		<tr>
			<th>
				Year</th>
			<th>
				Name</th>
			<th width="125">
				Alias</th>

			<th>
				Comments</th>
		</tr>
		<tr valign="top">
			<td>
				1986</td>
			<td>
				SQL-86</td>

			<td>
				SQL-87</td>
			<td>
				First formalized by ANSI.</td>
		</tr>
		<tr valign="top">
			<td>
				1989</td>

			<td>
				SQL-89</td>
			<td>
				<span>FIPS</span> 127-1</td>
			<td>
				Minor revision, adopted as FIPS 127-1.</td>

		</tr>
		<tr valign="top">
			<td>
				1992</td>
			<td>
				<span>SQL-92</span></td>
			<td>
				SQL2, FIPS 127-2</td>

			<td>
				Major revision (ISO 9075), <i>Entry Level</i> SQL-92 adopted as FIPS 127-2.</td>
		</tr>
		<tr valign="top">
			<td>
				1999</td>

			<td>
				<span>SQL:1999</span></td>
			<td>
				SQL3</td>
			<td>
				Added regular expression matching, recursive queries, <span>triggers</span>, support for procedural and control-of-flow statements, non-scalar types, and some object-oriented features.</td>

		</tr>
		<tr valign="top">
			<td>
				2003</td>
			<td>
				<span>SQL:2003</span></td>
			<td>
				SQL 2003</td>

			<td>
				Introduced <span>XML</span>-related features, <i>window functions</i>, standardized sequences, and columns with auto-generated values (including identity-columns).</td>
		</tr>
		<tr valign="top">
			<td>
				2006</td>

			<td>
				SQL:2006</td>
			<td>
				SQL 2006</td>
			<td>
				ISO/IEC 9075-14:2006 defines ways in which SQL can be used in conjunction with XML. It defines ways of importing and storing XML data in an SQL database, manipulating it within the database and publishing both XML and conventional SQL-data in XML form. In addition, it enables applications to integrate into their SQL code the use of <span>XQuery</span>, the XML Query Language published by the World Wide Web Consortium (<span class="mw-redirect">W3C</span>), to concurrently access ordinary SQL-data and XML documents.<sup style="display:none;" class="reference" id="cite_ref-SQLXML2006_23-0"><span><span>[</span>24<span>]</span></span></sup></td>

		</tr>
		<tr valign="top">
			<td>
				2008</td>
			<td>
				<span>SQL:2008</span></td>
			<td>
				SQL 2008</td>

			<td>
				Legalizes ORDER BY outside cursor definitions. Adds INSTEAD OF triggers. Adds the TRUNCATE statement.<sup style="display:none;" class="reference" id="cite_ref-iablog.sybase.com-paulley_24-0"><span><span>[</span>25<span>]</span></span></sup></td>
		</tr>
	</tbody>
</table>
<p>
	Interested parties may purchase SQL standards documents from ISO or ANSI. A draft of SQL:2008 is freely available as a <span>zip</span> archive.<sup style="display:none;" class="reference" id="cite_ref-25"><span><span>[</span>26<span>]</span></span></sup></p>

<h3>
	<span class="mw-headline" id="Standard_structure">Standard structure</span></h3>
<p>
	The SQL standard is divided into several parts, including:</p>
<p>
	SQL Framework, provides logical concept</p>
<p>
	SQL/Foundation, defined in ISO/IEC 9075, Part 2. This part of the standard contains the most central elements of the language. It consists of both <b>mandatory and optional</b> features.</p>

<p>
	The SQL/Bindings, specifies how SQL is to be bound to variable host languages, excluding Java.</p>
<p>
	The <span>SQL/CLI</span>, or <b>Call-Level Interface</b>, part is defined in ISO/IEC 9075, Part 3. SQL/CLI defines common interfacing components (structures and procedures) that can be used to execute SQL statements from applications written in other programming languages. SQL/CLI is defined in such a way that SQL statements and SQL/CLI procedure calls are treated as separate from the calling application's source code. <span>Open Database Connectivity</span> is a well-known superset of SQL/CLI. This part of the standard consists solely of <b>mandatory</b> features.</p>

<p>
	The <span>SQL/PSM</span>, or <b>Persistent Stored Modules</b>, part is defined by ISO/IEC 9075, Part 4. SQL/PSM standardizes procedural extensions for SQL, including flow of control, condition handling, statement condition signals and resignals, cursors and local variables, and assignment of expressions to variables and parameters. In addition, SQL/PSM formalizes declaration and maintenance of persistent database language routines (e.g., "stored procedures"). This part of the standard consists solely of <b>optional</b> features.</p>
<p>
	The <span>SQL/MED</span>, or <b>Management of External Data</b>, part is defined by ISO/IEC 9075, Part 9. SQL/MED provides extensions to SQL that define foreign-data wrappers and datalink types to allow SQL to manage external data. External data is data that is accessible to, but not managed by, an SQL-based DBMS. This part of the standard consists solely of <b>optional</b> features.</p>

<p>
	The <span>SQL/OLB</span>, or <b>Object Language Bindings</b>, part is defined by ISO/IEC 9075, Part 10. SQL/OLB defines the syntax and symantics of <span>SQLJ</span>, which is SQL embedded in Java. The standard also describes mechanisms to ensure binary portability of SQLJ applications, and specifies various Java packages and their contained classes. This part of the standard consists solely of <b>optional</b> features.</p>
<p>
	The SQL/MM (Multimedia), This extends SQL to deal intelligently with large, complex and sometimes streaming items of data, such as video, audio and spatial data.</p>

<p>
	The <span>SQL/Schemata</span>, or <b>Information and Definition Schemas</b>, part is defined by ISO/IEC 9075, Part 11. SQL/Schemata defines the Information Schema and Definition Schema, providing a common set of tools to make SQL databases and objects self-describing. These tools include the SQL object identifier, structure and integrity constraints, security and authorization specifications, features and packages of ISO/IEC 9075, support of features provided by SQL-based DBMS implementations, SQL-based DBMS implementation information and sizing items, and the values supported by the DBMS implementations.<sup style="display:none;" class="reference" id="cite_ref-ISO.2FIEC_9075-11:2008_26-0"><span><span>[</span>27<span>]</span></span></sup> This part of the standard contains both <b>mandatory and optional</b> features.</p>
<p>

	The <span>SQL/JRT</span>, or <b>SQL Routines and Types for the Java Programming Language</b>, part is defined by ISO/IEC 9075, Part 13. SQL/JRT specifies the ability to invoke static Java methods as routines from within SQL applications. It also calls for the ability to use Java classes as SQL structured user-defined types. This part of the standard consists solely of <b>optional</b> features.</p>
<p>
	The <span>SQL/XML</span>, or <b>XML-Related Specifications</b>, part is defined by ISO/IEC 9075, Part 14. SQL/XML specifies SQL-based extensions for using XML in conjunction with SQL. The XML data type is introduced, as well as several routines, functions, and XML-to-SQL data type mappings to support manipulation and storage of XML in an SQL database.<sup style="display:none;" class="reference" id="cite_ref-SQLXML2006_23-1"><span><span>[</span>24<span>]</span></span></sup> This part of the standard consists solely of <b>optional</b> features.</p>
<!-- end content here -->

<jsp:include page="contentFooter.jsp"/>